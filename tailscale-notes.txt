ssh check mode (https://tailscale.com/kb/1193/tailscale-ssh#configure-tailscale-ssh-with-check-mode)
- allows for mfa, but leaving it out for now

tailscale ssh is fire

do ssh tests later

how does gang want to auth new contributors?

how do we verify the code being uploaded isnt evil?

are we concerned about dos attacks? how do we prevent them

should we restrict what can be executed on the target? how? (groups?)

should the ci actions be used in the future? does this interfere with the goal of not exposing network to the greater internet?